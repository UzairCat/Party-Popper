# Railway supplies PORT automatically. This is only needed for local production testing.
PORT=3000

# Backend only. Never prefix API secrets with VITE_.
OPENAI_API_KEY=
# Optional: defaults to this model when omitted.
OPENAI_QUIZ_MODEL=gpt-4.1-mini
